import {BrowserRouter, Routes, Route} from 'react-router-dom';
import {Box} from '@mui/material';



const app = () => {
  <BrowserRouter>
    <Box>
      <Routes>
        
      </Routes>
    </Box>
  </BrowserRouter>
}

export default app